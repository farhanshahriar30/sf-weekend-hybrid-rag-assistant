[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sf-weekend-hybrid-rag-assistant"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
# Explicit: only package the Python package(s) we want
packages = ["app"]
